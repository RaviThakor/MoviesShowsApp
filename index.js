/**
 * @format
 */

import {AppRegistry} from 'react-native';
import NavigationStack from './src/navigations/stacknavigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => NavigationStack);
